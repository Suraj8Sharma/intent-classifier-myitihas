# Developer tooling — installs the full runtime stack plus test/lint tools.
-r requirements.txt

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0            # FastAPI TestClient / async HTTP in tests

# --- Formatting & linting ---
black>=24.4.0
flake8>=7.0.0
