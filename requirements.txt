# =============================================================================
# Local AI routing pipeline — self-hosted stack (LangGraph + ChromaDB + Ollama)
# No cloud/hosted LLM SDKs. The OpenAI client below targets the local Ollama
# OpenAI-compatible endpoint (http://localhost:11434/v1), not api.openai.com.
# =============================================================================

# --- Orchestration ---
langgraph>=0.2.0,<1.0.0

# --- Structured outputs ---
instructor>=1.5.0,<2.0.0

# --- Local LLM client (Ollama OpenAI-compatible API layer) ---
openai>=1.40.0

# --- Data validation (Pydantic v2) ---
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0

# --- Local vector store ---
chromadb>=0.5.0

# --- API layer ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# --- Config & logging ---
pyyaml>=6.0.1,<7.0.0
python-dotenv>=1.0.0,<2.0.0
structlog>=24.1.0
